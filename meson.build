project('lists', 'cpp',
        version : '1.0.0',
        license : 'GPL-3.0-or-later',
        default_options : ['warning_level=3'])

inc = include_directories('include')

subdir('include')
subdir('src')
