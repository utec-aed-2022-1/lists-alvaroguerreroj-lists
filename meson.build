project('lists', 'cpp',
        version : '1.0.0',
        license : 'GPL-3.0-or-later')

add_global_arguments('-DWall', language : 'cpp')
add_global_arguments('-DWextra', language : 'cpp')

inc = include_directories('include')

subdir('include')
subdir('src')
